package com.hussainIT.thread;

import java.util.HashSet;
import java.util.Set;
import java.util.stream.Collectors;

public class Demo {
    public static void main(String[] args) {
    	 A a = new B();
         System.out.println(a.i);
         String str="GoodMorning";

         Set<Character> set=new HashSet<>();

         Set<Character> collect = str.chars().mapToObj(c->(char)c).filter(f->set.add(f)).collect(Collectors.toSet());
         String collect2 = str.chars().mapToObj(c->(char)c).filter(set::add).map(String::valueOf).collect(Collectors.joining());
         
         System.err.println(collect2);

   
         

	}
}
class A
{
    int i = 10;
}
class B extends A
{
    int i = 20;
}


   
